<table border="3">
    <tr>
        <td>id</td>
        <td>username</td>
        <td>email</td>
        <td>Nombre des livres</td>
        <td colspan="3">Action</td>
        
    </tr>
    {% for a in authors %}
    <tr>
        <td>{{a.id}}</td>
        <td>{{a.username | upper}}</td>
        <td>{{a.email}}</td>
        <td>{{a.nbbooks}}</td>
        <td><a href="{{path('editauthor',{'id':a.id})}}">edit</a></td>
        <td><a href="{{path('deleteauthor',{'id':a.id})}}">delete</a></td>
{#        <td><a href="{{path('showbookAuthor',{'id':a.id})}}">books</a></td>#}
{#        <td><a href="{{path('showbookAuthor',{'id':a.id})}}">liste des Livres</a></td>#}
    </tr>
    {% endfor %}
</table>